package com.generation.navigationcomponent

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        /*
        Componente de Navegação (Navigation Component)

        NavGraph - Onde faremos o gráfico de navegações por meio de setas

        NavHostFragment - Será o container onde exibiremos os Fragments

        NavController - Fará a navegação com base no NavGraph

         */

    }
}